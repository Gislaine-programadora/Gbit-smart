{
  "name": "hardhat-starter",
  "version": "1.0.0",
  "description": "Template padrão gerado pelo gbit create — Projeto Hardhat completo",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.5",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-waffle": "^2.0.6",
    "ethers": "^6.11.1"
  }
}
